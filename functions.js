/* Functions // Funçôes
  Trecho de código que é executado
  Somente quando for solicitado (Parente gente boa)
*/
// Exemplo de função simples
function saudacao() {
    console.log("Olá, eu sou uma função!");
    }
    saudacao(); // Chama a função para mostrar o resultado na tela
    /*
    Ao chamar a função acima, o resultado será: Olá, eu sou uma função! 
    */
   /* Parâmetros e Retorno
   -------------------
   Uma função pode receber dados como entrada através dos parâmetros.   
   Eles são usados para personalizar a execução da função com valores específicos.
   */
  function somar(x, y){
    return x + y;
    }
    var res = somar(5,10);
    console.log(res); // Mostra o valor retornado pela função "somar"

    const variavel ="abacatinho"

    console.log(variavel)

function nomeNaTela() {
    console.log("Rodolfo")
}

nomeNaTela()
nomeNaTela()
nomeNaTela()
nomeNaTela()
nomeNaTela()
nomeNaTela()

// Posso chamar a funçâo quantas vezes eu quiser e for necessário.